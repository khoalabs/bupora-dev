🧠 Brand Ecosystem (solo, content-first)

	🌐 domain.com → hub (traffic + trust + email)
		breeds → traffic by interest (SEO + Pinterest)
    care → traffic by problem (SEO + affiliate)
    lifestyle → emotion → merch bridge
    shop → shop.domain.com
	
	✉ email → core asset (ownership)
    subscriber (from site content)
    buyer (site ebook + shop merch)
    flow:
			welcome
			story
			bridge → product
			soft sell
	
	🛒 shop.domain.com (fourthwall)
    → cashflow + fulfillment layer
    → merch only
    menu = shop | breeds | gifts
	
	🚀 traffic engine
    pinterest → scale traffic (pin → site → email → shop)
			inspiration pin (viral)
			problem pin (search)
			soft product pin (intent)
    youtube → build trust
			short (reach)
			long (depth)
			→ merch shelf (fourthwall)
			
	🔁 core flow:
    content → trust → emotion → email → purchase

		youtube → site / shop (direct)
		pinterest → site
		site → email (subscriber / ebook) → offer → shop
		shop → email (merch)
